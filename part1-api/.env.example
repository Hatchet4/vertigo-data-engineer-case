# Local docker-compose example connection string (if you spin up local Postgres):
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/vertigo

# For Cloud Run, inject via Secret Manager or env var:
# DATABASE_URL=postgresql+psycopg2://<USER>:<PASSWORD>@/<DB>?host=/cloudsql/<PROJECT:REGION:INSTANCE>